export default function () {
  return {
    // текущий мастер
    currentPerformer: null,
    isLoading: false,
    error: null,
    // Отзывы по текущему мастеру
    reviewsCurrentPerformer: [],
  }
}
